#include <iostream>
#include <string>
#include <fstream>

using namespace std;

void readFile (ifstream& weatherFile, char monthDays [3][30]); 
void countDays (char monthDays [3][30]);

int main() {

char monthDays [3][30];

ifstream weatherFile ("RainOrShine.txt");

return 0;

} 
void readFile (ifstream& weatherFile, char monthDays [3][30]);

int column = 0;
int row = 0;

char forecast;

while (myFile >> weather) {
    
    if [weather = 'R', weather = 'S', weather = 'C'] {
        
        monthDays[row][column++] = forecast;
    }
    
}
