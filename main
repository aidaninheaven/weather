#include <iostream>
#include <string>
#include <fstream>

void juneStats(int& juneRain, int& juneSun, int& juneCloud);
void julyStats(int& julyRain, int& julySun, int& julyCloud);
void augStats (int& augRain, int& augSun, int& augCloud);

//void totalStats (int& totalRain,int& totalSun, int& totalCloud);

//void readFile 
//void printData

int main() {
    
    int juneRain = 0 
    int juneSun = 0
    int juneCloud = 0 
    
    int julyRain = 0
    int julySun = 0
    int julyCloud = 0
    
    int augRain = 0
    int augSun = 0
    int augCloud = 0
    
   // int totalRain = 0
   // int totalSun = 0 
   // int totalCloud = 0 
   
    return 0;
}

//void juneStats(int& juneRain, int& juneSun, int& juneCloud);

//void julyStats(int& julyRain = 0, int& julySun, int& julyCloud);

//void augStats(int& augRain = 0, int& augSun, int& augCloud);




//void totalStats (int& totalRain,int& totalSun, int& totalCloud);
